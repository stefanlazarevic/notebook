import PropTypes from 'prop-types';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean, number, date } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import CalendarGrid from '../index.ts';

<!-- Examples -->

<Meta title="Components/CalendarGrid" component={CalendarGrid} decorators={[withKnobs]} />

# CalendarGrid

- Када је одабрани дан дефинисан, компонента поставља иницијални фокус на одабрани дан.
- Када одабрани дан није дефинисан, компонента поставља иницијални фокус на данашњни дан.

<Preview>
    <Story name="Preview">
        <CalendarGrid 
            month={number('month', 4)} 
            year={number('year', 2016)} 
            onSelect={action('onSelect')}
            selectedDay={number('selectedDay', 14)}
            selectedMonth={number('selectedMonth', 4)}
            selectedYear={number('selectedYear', 2016)}
        />
    </Story>
</Preview>