import PropTypes from 'prop-types';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, number, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import Switch from '../index.ts';

<!-- Examples -->
import ExampleCheckbox from '../examples/Example/Example.tsx';

<Meta title="Components/Switch" component={Switch} decorators={[withKnobs]}/>

# Switch

`Switch` представља тип `Checkbox` компоненте која поседује само два стања, укључено и искључено.

<Preview>
    <Story name="Preview">
        <Switch 
            checked={boolean('checked', true)}
            onChange={action('onChange')}
        />
    </Story>
</Preview>

### Атрибути

<Props of="Switch" />

## Радни примери

### Основни пример

<Preview>
    <Story name="Example">
        <ExampleCheckbox />
    </Story>
</Preview>