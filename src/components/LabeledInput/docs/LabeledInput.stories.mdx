import PropTypes from 'prop-types';
import { Meta, Story, Preview, Props, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { withKnobs, number, text, boolean, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import LabeledInput from '../index.ts';

<Meta title="Components/LabeledInput" component={LabeledInput} decorators={[withKnobs]}/>

# LabeledInput

<Preview>
    <Story name="Preview">
        <LabeledInput>Label</LabeledInput>
    </Story>
</Preview>

## Атрибути

<Props of={LabeledInput} />