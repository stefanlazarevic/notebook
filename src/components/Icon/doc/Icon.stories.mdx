import PropTypes from 'prop-types';
import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { withKnobs, number, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import Icon from "../Icon";
import Icons from "../Icons";

<Meta title="Components/Icon" component={Icon} decorators={[withKnobs]}/>

# Icons

Компонента за приказивање `svg` иконица.

Све иконице у овој библиотеци су преузете са: [lineicons.com](https://lineicons.com/icons/)

<Preview>
  <Story name="Preview">
    <Icon icon={select("icon", Object.values(Icons), Object.values(Icons)[0])} size={number("size", 48)} />
  </Story>
</Preview>
