import PropTypes from 'prop-types';
import { Meta, Story, Preview, ColorPalette, ColorItem } from '@storybook/addon-docs/blocks';
import { withKnobs, number, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import LabeledCheckbox from '../index.ts';

<Meta title="Components/LabeledCheckbox" component={LabeledCheckbox} decorators={[withKnobs]}/>

# LabeledCheckbox

<Story name="Preview">
    <LabeledCheckbox checked={boolean('checked', false)} index={number('index', -1)} onChange={action('onChange')}>
        Some label
    </LabeledCheckbox>
</Story>