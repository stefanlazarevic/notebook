import PropTypes from 'prop-types';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import ConfirmationCodeInput from '../index.ts';

<!-- Examples -->

<Meta title="Components/Input/ConfirmationCodeInput" component={ConfirmationCodeInput} decorators={[withKnobs]} />

# ConfirmationCodeInput

<Preview>
    <Story name="Preview">
        <ConfirmationCodeInput onChange={action('onChange')} />
    </Story>
</Preview>

## Атрибути

<Props of={ConfirmationCodeInput} />