import PropTypes from 'prop-types';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, number, text, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";

import PasswordInput from '../index.ts';

<!-- Examples -->
import BasicExample from '../examples/BasicExample';

<Meta title="Components/Input/PasswordInput" component={PasswordInput} decorators={[withKnobs]} />

# PasswordInput

<Preview>
    <Story name="Preview">
        <PasswordInput value={text('value', '')} placeholder={text('placeholder', '')} onChange={action('onChange')} onFocus={action('onFocus')} onBlur={action('onBlur')} invalid={boolean('invalid', false)} disabled={boolean('disabled', false)} />
    </Story>
</Preview>

## Атрибути

<Props of={PasswordInput} />

## Радни примери

### Основни пример

<Story name="Example (Basic)">
    <BasicExample />
</Story>